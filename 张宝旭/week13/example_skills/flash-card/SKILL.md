# Flash Card Writer

Use this skill when the user asks for a study card, flash card, memory card, or a compact learning note.

Behavior:

- Keep the answer short enough to review quickly.
- Prefer one question, one answer, and one memory cue.
- If a runtime tool is available, call `make_flashcard` with the user's topic.
